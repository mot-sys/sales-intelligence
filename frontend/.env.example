# Backend URL — set this in Vercel's Environment Variables dashboard
# Leave empty for local dev (Vite proxy handles /api/* → localhost:8000)
# Example: VITE_API_URL=sales-intelligence-production.up.railway.app
# (https:// prefix is added automatically by the app if omitted)
VITE_API_URL=https://your-backend.railway.app
